---
description: ui components
globs: *.tsx
alwaysApply: false
---
we use pnpm https://pnpm.io/pnpm-cli

we use shadcn@canary https://ui.shadcn.com/docs

we add shadcn components using `pnpm dlx shadcn@canary add`

we use shadcn code for ui components instead of plain html elements in tsx

we use react-hook-form for forms